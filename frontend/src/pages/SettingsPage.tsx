import { PlaceholderPage } from '../components/PlaceholderPage';

export function SettingsPage() {
  return (
    <PlaceholderPage
      eyebrow="Settings"
      title="User and Platform Preferences"
      description="Keep the configuration layer minimal, secure, and role-aware while the core intelligence workflows stay front and center."
      primaryActionLabel="Update profile, language, and notifications"
      secondaryActionLabel="Review session and access settings"
    />
  );
}
